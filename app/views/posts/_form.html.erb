<%= form_for post do |f| %>

  <%= f.text_field :name %>
  <%= f.text_area :content %>
  <%= f.collection_select(:category_id, @categories, :id, :name) %>
  <%= f.submit post.new_record? ? 'Créer' : "editer" %>

<% end %>